<h1>新規作成</h1>

<%= form_for(@list, url: '/todolists') do |f| %>

	<h4>タイトル</h4>
	<%= f.text_field :title %>

	<h4>本文</h4>
	<%= f.text_area :body %>

	<h4>画像</h4>
	<%= f.attachment_field :image %>

	<%= f.submit '保存' %>

<% end %>

<%= link_to "一覧へ戻る", "/todolists" %>
